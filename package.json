{
  "devDependencies": {
    "@dmsi/prettier-config": "^1.3.0",
    "cheerio": "^1.0.0-rc.12",
    "husky": "^8.0.3",
    "lint-staged": "^13.1.0",
    "prettier": "^2.8.3"
  },
  "type": "module",
  "scripts": {
    "prepare": "husky install",
    "lint-stage": "lint-staged",
    "build": "node build.js"
  },
  "lint-staged": {
    "*.{html,css}": [
      "prettier --write"
    ]
  },
  "prettier": "@dmsi/prettier-config",
  "dependencies": {
    "papaparse": "^5.3.2"
  }
}
